set(rmntools_VERSION @PROJECT_VERSION@ CACHE INTERNAL "rmntools version")

include(CMakeFindDependencyMacro)

if(@WITH_OPENMP@)
    find_dependency(OpenMP REQUIRED)
endif()

include(${CMAKE_CURRENT_LIST_DIR}/rmntools-targets.cmake)
